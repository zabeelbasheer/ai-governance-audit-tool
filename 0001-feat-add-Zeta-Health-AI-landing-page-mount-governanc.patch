From 2a687b8eebaa9f4d2ec6bd0ba2bc3943fe4fe1fb Mon Sep 17 00:00:00 2001
From: zabeelbasheer <53309631+zabeelbasheer@users.noreply.github.com>
Date: Sat, 22 Aug 2026 16:03:42 +0000
Subject: [PATCH] feat: add Zeta Health AI landing page, mount governance audit
 app under /governance-audit

---
 governance_audit_app.py | 101 +++++++++++++++++++++++++++++++
 index.html              |   8 +--
 landing.html            | 131 ++++++++++++++++++++++++++++++++++++++++
 main.py                 | 101 ++++---------------------------
 4 files changed, 247 insertions(+), 94 deletions(-)
 create mode 100644 governance_audit_app.py
 create mode 100644 landing.html

diff --git a/governance_audit_app.py b/governance_audit_app.py
new file mode 100644
index 0000000..3e5b7b3
--- /dev/null
+++ b/governance_audit_app.py
@@ -0,0 +1,101 @@
+"""
+main.py — Zeta Health AI · Governance Audit
+FastAPI + vanilla JS web layer, phase 1 of the Streamlit migration.
+
+Phase 1 scope: dashboard shell, session-cookie auth, session list, search.
+Reuses db.py and auth.verify_credentials unchanged. The Streamlit app.py
+and pages/ directory are left in place until later phases are ported.
+"""
+
+import os
+from contextlib import asynccontextmanager
+
+from fastapi import FastAPI, Request, HTTPException
+from fastapi.responses import FileResponse
+from pydantic import BaseModel
+from starlette.middleware.sessions import SessionMiddleware
+
+from db import init_db, get_sessions_for_user, get_all_sessions
+from auth import verify_credentials, ROLE_LABELS
+
+
+@asynccontextmanager
+async def lifespan(app: FastAPI):
+    init_db()
+    yield
+
+app = FastAPI(title="Zeta Health AI · Governance Audit", lifespan=lifespan)
+app.add_middleware(
+    SessionMiddleware,
+    secret_key=os.getenv("SESSION_SECRET", "dev-secret-change-in-production"),
+)
+
+
+def can_view_all(role: str) -> bool:
+    """Auditors, DPOs and admins can see all sessions. Mirrors auth.can_view_all."""
+    return role in ("auditor", "dpo", "admin")
+
+
+class LoginBody(BaseModel):
+    email: str
+    password: str
+
+
+@app.post("/api/login")
+def login(body: LoginBody, request: Request):
+    user = verify_credentials(body.email, body.password)
+    if not user:
+        raise HTTPException(
+            status_code=401,
+            detail="Incorrect email or password. Contact your administrator if you need access.",
+        )
+    session_user = {
+        "id": user["id"],
+        "email": user["email"],
+        "display_name": user["display_name"],
+        "role": user["role"],
+    }
+    request.session["user"] = session_user
+    return {"user": session_user, "role_label": ROLE_LABELS[user["role"]]}
+
+
+@app.post("/api/logout")
+def logout(request: Request):
+    request.session.clear()
+    return {"ok": True}
+
+
+@app.get("/api/me")
+def me(request: Request):
+    user = request.session.get("user")
+    if not user:
+        raise HTTPException(status_code=401, detail="Not signed in.")
+    return {"user": user, "role_label": ROLE_LABELS[user["role"]]}
+
+
+@app.get("/api/sessions")
+def sessions(request: Request, q: str = ""):
+    user = request.session.get("user")
+    if not user:
+        raise HTTPException(status_code=401, detail="Not signed in.")
+
+    rows = get_all_sessions() if can_view_all(user["role"]) else get_sessions_for_user(user["id"])
+
+    if q:
+        ql = q.lower()
+        rows = [
+            r for r in rows
+            if ql in (r.get("title") or "").lower()
+            or ql in (r.get("display_name") or "").lower()
+            or ql in (r.get("email") or "").lower()
+            or ql in str(r.get("overall_pct") or "").lower()
+            or ql in (r.get("maturity_label") or "").lower()
+            or ql in (r.get("vendor_name") or "").lower()
+        ]
+
+    return {"sessions": rows, "can_view_all": can_view_all(user["role"])}
+
+
+@app.get("/")
+def index():
+    return FileResponse("index.html")
diff --git a/index.html b/index.html
index 4d85ea0..9863314 100644
--- a/index.html
+++ b/index.html
@@ -368,7 +368,7 @@ function renderSessions(data) {
 }
 
 async function loadSessions(q = '') {
-  const data = await api('/api/sessions' + (q ? `?q=${encodeURIComponent(q)}` : ''));
+  const data = await api('api/sessions' + (q ? `?q=${encodeURIComponent(q)}` : ''));
   renderSessions(data);
 }
 
@@ -382,7 +382,7 @@ function showDashboard(user, roleLabel) {
 
 async function checkSession() {
   try {
-    const data = await api('/api/me');
+    const data = await api('api/me');
     showDashboard(data.user, data.role_label);
   } catch {
     loginView.classList.remove('hidden');
@@ -400,7 +400,7 @@ document.getElementById('login-btn').addEventListener('click', async () => {
     return;
   }
   try {
-    const data = await api('/api/login', { method: 'POST', body: JSON.stringify({ email, password }) });
+    const data = await api('api/login', { method: 'POST', body: JSON.stringify({ email, password }) });
     showDashboard(data.user, data.role_label);
   } catch (err) {
     loginError.textContent = err.message;
@@ -413,7 +413,7 @@ document.getElementById('password').addEventListener('keydown', e => {
 });
 
 document.getElementById('signout-btn').addEventListener('click', async () => {
-  await api('/api/logout', { method: 'POST' });
+  await api('api/logout', { method: 'POST' });
   location.reload();
 });
 
diff --git a/landing.html b/landing.html
new file mode 100644
index 0000000..adf41d3
--- /dev/null
+++ b/landing.html
@@ -0,0 +1,131 @@
+<!DOCTYPE html>
+<html lang="en">
+<head>
+<meta charset="UTF-8">
+<meta name="viewport" content="width=device-width, initial-scale=1.0">
+<title>Zeta Health AI</title>
+<style>
+*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
+
+:root {
+  --midnight:  #0f1e2d;
+  --navy:      #1a3347;
+  --gold:      #e8a020;
+  --amber:     #b8760a;
+  --parchment: #f7f4ee;
+  --sky:       #8fa8bf;
+  --ocean:     #2a5070;
+  --border:    #d8d4cc;
+  --white:     #ffffff;
+}
+
+body {
+  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+  background: var(--parchment);
+  color: var(--midnight);
+}
+
+.wrap { max-width: 880px; margin: 0 auto; padding: 48px 24px 80px; }
+
+.brand { display: flex; align-items: center; gap: 14px; margin-bottom: 40px; }
+.eyebrow { font-size: 10px; color: var(--amber); letter-spacing: 2.5px; font-weight: 700; }
+.wordmark { font-size: 26px; font-weight: 700; color: var(--midnight); font-family: Georgia, serif; }
+.tagline { font-size: 13px; color: var(--ocean); margin-top: 2px; }
+
+.about-card {
+  background: var(--white);
+  border-radius: 12px;
+  padding: 28px 32px;
+  margin-bottom: 32px;
+}
+.about-name { font-size: 20px; font-weight: 700; color: var(--midnight); font-family: Georgia, serif; }
+.about-role { font-size: 13px; color: var(--ocean); margin-top: 4px; }
+.about-body { font-size: 14px; line-height: 1.7; color: var(--midnight); margin-top: 16px; }
+.about-links { margin-top: 18px; display: flex; gap: 16px; }
+.about-links a { font-size: 12px; color: var(--amber); text-decoration: none; font-weight: 700; }
+.about-links a:hover { color: var(--gold); }
+
+.section-label { font-size: 11px; color: var(--sky); letter-spacing: 1.5px; text-transform: uppercase; margin-bottom: 14px; }
+
+.projects { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 16px; }
+
+.project-card {
+  background: var(--white);
+  border-radius: 12px;
+  padding: 22px 24px;
+  text-decoration: none;
+  color: inherit;
+  display: block;
+  border: 1px solid var(--border);
+}
+.project-card:hover { border-color: var(--gold); }
+
+.project-status {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  font-size: 10px;
+  font-weight: 700;
+  letter-spacing: 0.5px;
+  text-transform: uppercase;
+  color: #27ae60;
+  margin-bottom: 10px;
+}
+.project-status::before { content: ""; width: 6px; height: 6px; border-radius: 50%; background: #27ae60; }
+
+.project-title { font-size: 16px; font-weight: 700; color: var(--midnight); }
+.project-desc { font-size: 13px; color: var(--ocean); margin-top: 6px; line-height: 1.5; }
+.project-stack { font-size: 11px; color: var(--sky); margin-top: 12px; }
+</style>
+</head>
+<body>
+<div class="wrap">
+
+  <div class="brand">
+    <svg width="46" height="46" viewBox="0 0 22 22">
+      <circle cx="11" cy="11" r="11" fill="#1a3347"/>
+      <line x1="6" y1="7" x2="16" y2="7" stroke="#e8a020" stroke-width="1.6" stroke-linecap="round"/>
+      <path d="M14.5 7 Q16 11 6 15" fill="none" stroke="#e8a020" stroke-width="1.6" stroke-linecap="round"/>
+      <line x1="6" y1="15" x2="16" y2="15" stroke="#e8a020" stroke-width="1.6" stroke-linecap="round"/>
+    </svg>
+    <div>
+      <div class="eyebrow">ZETA HEALTH AI</div>
+      <div class="wordmark">Zeta Health AI</div>
+      <div class="tagline">Clinical operations, intelligently governed.</div>
+    </div>
+  </div>
+
+  <div class="about-card">
+    <div class="about-name">Zabeel M. Basheer</div>
+    <div class="about-role">VP, Business Excellence &amp; India Site Lead, Shearwater Health</div>
+    <div class="about-body">
+      20+ years across healthcare, BFS, and HR shared services. Built Shearwater's India GCC from 18 to 250+ headcount in 13 months. $20M+ cumulative value delivered through Lean Six Sigma-led transformation programs. Current focus: applied AI in healthcare operations, clinical intelligence, workforce management, and enterprise AI governance.
+    </div>
+    <div class="about-links">
+      <a href="https://github.com/zabeelbasheer" target="_blank" rel="noopener">GitHub</a>
+      <a href="https://linkedin.com/in/zabeelbasheer" target="_blank" rel="noopener">LinkedIn</a>
+    </div>
+  </div>
+
+  <div class="section-label">Live projects</div>
+  <div class="projects">
+
+    <a class="project-card" href="https://zeta-policy-assistant.onrender.com" target="_blank" rel="noopener">
+      <div class="project-status">Live</div>
+      <div class="project-title">HR Policy RAG Chatbot</div>
+      <div class="project-desc">Retrieval-augmented HR policy assistant with source-cited answers and a sensitivity-aware guard layer.</div>
+      <div class="project-stack">FastAPI · Groq · FAISS</div>
+    </a>
+
+    <a class="project-card" href="/governance-audit">
+      <div class="project-status">Live</div>
+      <div class="project-title">AI Governance Audit Tool</div>
+      <div class="project-desc">NIST AI RMF, HIPAA, and HITRUST risk scoring with vendor-aware baselines and remediation guidance.</div>
+      <div class="project-stack">FastAPI · Groq · SQLite</div>
+    </a>
+
+  </div>
+
+</div>
+</body>
+</html>
diff --git a/main.py b/main.py
index 3e5b7b3..8218b8e 100644
--- a/main.py
+++ b/main.py
@@ -1,101 +1,22 @@
 """
-main.py — Zeta Health AI · Governance Audit
-FastAPI + vanilla JS web layer, phase 1 of the Streamlit migration.
+main.py — Zeta Health AI portfolio hub.
 
-Phase 1 scope: dashboard shell, session-cookie auth, session list, search.
-Reuses db.py and auth.verify_credentials unchanged. The Streamlit app.py
-and pages/ directory are left in place until later phases are ported.
+Serves the landing page at "/" and mounts individual project apps as
+sub-applications at their own path prefixes. This is the consolidation
+point so future projects add one mount line each instead of a separate
+paid Render service per project.
 """
 
-import os
-from contextlib import asynccontextmanager
-
-from fastapi import FastAPI, Request, HTTPException
+from fastapi import FastAPI
 from fastapi.responses import FileResponse
-from pydantic import BaseModel
-from starlette.middleware.sessions import SessionMiddleware
-
-from db import init_db, get_sessions_for_user, get_all_sessions
-from auth import verify_credentials, ROLE_LABELS
-
-
-@asynccontextmanager
-async def lifespan(app: FastAPI):
-    init_db()
-    yield
-
-app = FastAPI(title="Zeta Health AI · Governance Audit", lifespan=lifespan)
-app.add_middleware(
-    SessionMiddleware,
-    secret_key=os.getenv("SESSION_SECRET", "dev-secret-change-in-production"),
-)
-
-
-def can_view_all(role: str) -> bool:
-    """Auditors, DPOs and admins can see all sessions. Mirrors auth.can_view_all."""
-    return role in ("auditor", "dpo", "admin")
-
-
-class LoginBody(BaseModel):
-    email: str
-    password: str
-
-
-@app.post("/api/login")
-def login(body: LoginBody, request: Request):
-    user = verify_credentials(body.email, body.password)
-    if not user:
-        raise HTTPException(
-            status_code=401,
-            detail="Incorrect email or password. Contact your administrator if you need access.",
-        )
-    session_user = {
-        "id": user["id"],
-        "email": user["email"],
-        "display_name": user["display_name"],
-        "role": user["role"],
-    }
-    request.session["user"] = session_user
-    return {"user": session_user, "role_label": ROLE_LABELS[user["role"]]}
-
-
-@app.post("/api/logout")
-def logout(request: Request):
-    request.session.clear()
-    return {"ok": True}
-
-
-@app.get("/api/me")
-def me(request: Request):
-    user = request.session.get("user")
-    if not user:
-        raise HTTPException(status_code=401, detail="Not signed in.")
-    return {"user": user, "role_label": ROLE_LABELS[user["role"]]}
-
-
-@app.get("/api/sessions")
-def sessions(request: Request, q: str = ""):
-    user = request.session.get("user")
-    if not user:
-        raise HTTPException(status_code=401, detail="Not signed in.")
 
-    rows = get_all_sessions() if can_view_all(user["role"]) else get_sessions_for_user(user["id"])
+from governance_audit_app import app as governance_audit_app
 
-    if q:
-        ql = q.lower()
-        rows = [
-            r for r in rows
-            if ql in (r.get("title") or "").lower()
-            or ql in (r.get("display_name") or "").lower()
-            or ql in (r.get("email") or "").lower()
-            or ql in str(r.get("overall_pct") or "").lower()
-            or ql in (r.get("maturity_label") or "").lower()
-            or ql in (r.get("vendor_name") or "").lower()
-        ]
+app = FastAPI(title="Zeta Health AI")
 
-    return {"sessions": rows, "can_view_all": can_view_all(user["role"])}
+app.mount("/governance-audit", governance_audit_app)
 
 
 @app.get("/")
-def index():
-    return FileResponse("index.html")
+def landing():
+    return FileResponse("landing.html")
-- 
2.43.0

